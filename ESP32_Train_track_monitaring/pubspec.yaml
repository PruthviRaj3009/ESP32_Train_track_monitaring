name: loginpage
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  animate_do: ^3.1.2
  firebase_core: ^2.24.2
  firebase_auth: ^4.17.3
  # WebView (pinned to a version compatible with Dart 3.4.x)
  webview_flutter: ^4.8.0
  # Platform implementations (added as direct deps so we can use Android/iOS specific settings)
  webview_flutter_android: ^3.16.8
  webview_flutter_wkwebview: ^3.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
